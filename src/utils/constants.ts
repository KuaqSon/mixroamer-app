import { IMenu, IPaginatorRequest } from 'src/interfaces/common';

export const APP_NAME = 'mixroamer_app';
export const DEFAULT_PAGINATOR: IPaginatorRequest = {
  page: 1,
  limit: 50,
  terms: '',
};

export const PLAYER_WIDTH = 500;
export const PLAYER_HEIGHT = 80;
export const SIDEBAR_WIDTH = 280;
